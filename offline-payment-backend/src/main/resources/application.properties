# Activate 'prod' profile automatically when SPRING_PROFILES_ACTIVE is set (Render sets it via env var)
# Fallback: activate prod profile when PORT env var is present (Render always sets PORT)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:prod}

spring.application.name=offline-payment-backend
